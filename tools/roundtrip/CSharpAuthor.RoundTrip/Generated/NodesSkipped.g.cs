// <auto-generated> 10 Syntax.xml nodes that nodes.json drops (SkipConvenienceFactories).
// Generated by tools/roundtrip/gen_roundtrip.py from Roslyn's Syntax.xml.
// Field order in the grammar IS emit order; that is the whole trick.
#nullable enable
using System.Collections.Generic;
using CSharpAuthor;

namespace GeneratedGrammar.Full;

public sealed class UnsafeExpression : IExpression
{
    public IExpression? Expression { get; set; }

    public UnsafeExpression(IExpression expression)
    {
        Expression = expression;
    }

    public void AddUsingNamespace(string ns) { }

    public void WriteOutput(IOutputContext outputContext)
    {
        Tk.W(outputContext, "unsafe");
        Tk.W(outputContext, "(");
        Expression?.WriteOutput(outputContext);
        Tk.W(outputContext, ")");
    }
}

public sealed class AnonymousMethodExpression : IExpression
{
    public List<ISyntax> Modifiers { get; } = new();
    public ISyntax? ParameterList { get; set; }
    public IStatement? Block { get; set; }
    public IExpression? ExpressionBody { get; set; }

    public AnonymousMethodExpression(IStatement block)
    {
        Block = block;
    }

    public void AddUsingNamespace(string ns) { }

    public void WriteOutput(IOutputContext outputContext)
    {
        for (var i = 0; i < Modifiers.Count; i++)
        {
            Modifiers[i].WriteOutput(outputContext);
        }
        Tk.W(outputContext, "delegate");
        ParameterList?.WriteOutput(outputContext);
        Block?.WriteOutput(outputContext);
        ExpressionBody?.WriteOutput(outputContext);
    }
}

public sealed class SimpleLambdaExpression : IExpression
{
    public List<ISyntax> AttributeLists { get; } = new();
    public List<ISyntax> Modifiers { get; } = new();
    public ISyntax? Parameter { get; set; }
    public IStatement? Block { get; set; }
    public IExpression? ExpressionBody { get; set; }

    public SimpleLambdaExpression(ISyntax parameter, IStatement block, IExpression expressionBody)
    {
        Parameter = parameter;
        Block = block;
        ExpressionBody = expressionBody;
    }

    public void AddUsingNamespace(string ns) { }

    public void WriteOutput(IOutputContext outputContext)
    {
        for (var i = 0; i < AttributeLists.Count; i++)
        {
            AttributeLists[i].WriteOutput(outputContext);
        }
        for (var i = 0; i < Modifiers.Count; i++)
        {
            Modifiers[i].WriteOutput(outputContext);
        }
        Parameter?.WriteOutput(outputContext);
        Tk.W(outputContext, "=>");
        Block?.WriteOutput(outputContext);
        ExpressionBody?.WriteOutput(outputContext);
    }
}

public sealed class SwitchStatement : IStatement
{
    public List<ISyntax> AttributeLists { get; } = new();
    public IExpression? Expression { get; set; }
    public List<ISyntax> Sections { get; } = new();

    public SwitchStatement(IExpression expression)
    {
        Expression = expression;
    }

    public void AddUsingNamespace(string ns) { }

    public void WriteOutput(IOutputContext outputContext)
    {
        for (var i = 0; i < AttributeLists.Count; i++)
        {
            AttributeLists[i].WriteOutput(outputContext);
        }
        Tk.W(outputContext, "switch");
        Tk.W(outputContext, "(");
        Expression?.WriteOutput(outputContext);
        Tk.W(outputContext, ")");
        Tk.W(outputContext, "{");
        for (var i = 0; i < Sections.Count; i++)
        {
            Sections[i].WriteOutput(outputContext);
        }
        Tk.W(outputContext, "}");
    }
}

public sealed class ClassDeclaration : IMemberDeclaration
{
    public List<ISyntax> AttributeLists { get; } = new();
    public List<ISyntax> Modifiers { get; } = new();
    public string Identifier { get; set; }
    public ISyntax? TypeParameterList { get; set; }
    public ISyntax? ParameterList { get; set; }
    public ISyntax? BaseList { get; set; }
    public List<ISyntax> ConstraintClauses { get; } = new();
    public List<IMemberDeclaration> Members { get; } = new();

    public ClassDeclaration(string identifier)
    {
        Identifier = identifier;
    }

    public void AddUsingNamespace(string ns) { }

    public void WriteOutput(IOutputContext outputContext)
    {
        for (var i = 0; i < AttributeLists.Count; i++)
        {
            AttributeLists[i].WriteOutput(outputContext);
        }
        for (var i = 0; i < Modifiers.Count; i++)
        {
            Modifiers[i].WriteOutput(outputContext);
        }
        Tk.W(outputContext, "class");
        Tk.W(outputContext, Identifier);
        TypeParameterList?.WriteOutput(outputContext);
        ParameterList?.WriteOutput(outputContext);
        BaseList?.WriteOutput(outputContext);
        for (var i = 0; i < ConstraintClauses.Count; i++)
        {
            ConstraintClauses[i].WriteOutput(outputContext);
        }
        Tk.W(outputContext, "{");
        for (var i = 0; i < Members.Count; i++)
        {
            Members[i].WriteOutput(outputContext);
        }
        Tk.W(outputContext, "}");
        Tk.W(outputContext, ";");
    }
}

public sealed class StructDeclaration : IMemberDeclaration
{
    public List<ISyntax> AttributeLists { get; } = new();
    public List<ISyntax> Modifiers { get; } = new();
    public string Identifier { get; set; }
    public ISyntax? TypeParameterList { get; set; }
    public ISyntax? ParameterList { get; set; }
    public ISyntax? BaseList { get; set; }
    public List<ISyntax> ConstraintClauses { get; } = new();
    public List<IMemberDeclaration> Members { get; } = new();

    public StructDeclaration(string identifier)
    {
        Identifier = identifier;
    }

    public void AddUsingNamespace(string ns) { }

    public void WriteOutput(IOutputContext outputContext)
    {
        for (var i = 0; i < AttributeLists.Count; i++)
        {
            AttributeLists[i].WriteOutput(outputContext);
        }
        for (var i = 0; i < Modifiers.Count; i++)
        {
            Modifiers[i].WriteOutput(outputContext);
        }
        Tk.W(outputContext, "struct");
        Tk.W(outputContext, Identifier);
        TypeParameterList?.WriteOutput(outputContext);
        ParameterList?.WriteOutput(outputContext);
        BaseList?.WriteOutput(outputContext);
        for (var i = 0; i < ConstraintClauses.Count; i++)
        {
            ConstraintClauses[i].WriteOutput(outputContext);
        }
        Tk.W(outputContext, "{");
        for (var i = 0; i < Members.Count; i++)
        {
            Members[i].WriteOutput(outputContext);
        }
        Tk.W(outputContext, "}");
        Tk.W(outputContext, ";");
    }
}

public sealed class UnionDeclaration : IMemberDeclaration
{
    public List<ISyntax> AttributeLists { get; } = new();
    public List<ISyntax> Modifiers { get; } = new();
    public string Keyword { get; set; }
    public string Identifier { get; set; }
    public ISyntax? TypeParameterList { get; set; }
    public ISyntax? ParameterList { get; set; }
    public ISyntax? BaseList { get; set; }
    public List<ISyntax> ConstraintClauses { get; } = new();
    public List<IMemberDeclaration> Members { get; } = new();

    public UnionDeclaration(string keyword, string identifier)
    {
        Keyword = keyword;
        Identifier = identifier;
    }

    public void AddUsingNamespace(string ns) { }

    public void WriteOutput(IOutputContext outputContext)
    {
        for (var i = 0; i < AttributeLists.Count; i++)
        {
            AttributeLists[i].WriteOutput(outputContext);
        }
        for (var i = 0; i < Modifiers.Count; i++)
        {
            Modifiers[i].WriteOutput(outputContext);
        }
        Tk.W(outputContext, Keyword);
        Tk.W(outputContext, Identifier);
        TypeParameterList?.WriteOutput(outputContext);
        ParameterList?.WriteOutput(outputContext);
        BaseList?.WriteOutput(outputContext);
        for (var i = 0; i < ConstraintClauses.Count; i++)
        {
            ConstraintClauses[i].WriteOutput(outputContext);
        }
        Tk.W(outputContext, "{");
        for (var i = 0; i < Members.Count; i++)
        {
            Members[i].WriteOutput(outputContext);
        }
        Tk.W(outputContext, "}");
        Tk.W(outputContext, ";");
    }
}

public sealed class InterfaceDeclaration : IMemberDeclaration
{
    public List<ISyntax> AttributeLists { get; } = new();
    public List<ISyntax> Modifiers { get; } = new();
    public string Identifier { get; set; }
    public ISyntax? TypeParameterList { get; set; }
    public ISyntax? ParameterList { get; set; }
    public ISyntax? BaseList { get; set; }
    public List<ISyntax> ConstraintClauses { get; } = new();
    public List<IMemberDeclaration> Members { get; } = new();

    public InterfaceDeclaration(string identifier)
    {
        Identifier = identifier;
    }

    public void AddUsingNamespace(string ns) { }

    public void WriteOutput(IOutputContext outputContext)
    {
        for (var i = 0; i < AttributeLists.Count; i++)
        {
            AttributeLists[i].WriteOutput(outputContext);
        }
        for (var i = 0; i < Modifiers.Count; i++)
        {
            Modifiers[i].WriteOutput(outputContext);
        }
        Tk.W(outputContext, "interface");
        Tk.W(outputContext, Identifier);
        TypeParameterList?.WriteOutput(outputContext);
        ParameterList?.WriteOutput(outputContext);
        BaseList?.WriteOutput(outputContext);
        for (var i = 0; i < ConstraintClauses.Count; i++)
        {
            ConstraintClauses[i].WriteOutput(outputContext);
        }
        Tk.W(outputContext, "{");
        for (var i = 0; i < Members.Count; i++)
        {
            Members[i].WriteOutput(outputContext);
        }
        Tk.W(outputContext, "}");
        Tk.W(outputContext, ";");
    }
}

public sealed class EnumDeclaration : IMemberDeclaration
{
    public List<ISyntax> AttributeLists { get; } = new();
    public List<ISyntax> Modifiers { get; } = new();
    public string Identifier { get; set; }
    public ISyntax? BaseList { get; set; }
    public List<IMemberDeclaration> Members { get; } = new();

    public EnumDeclaration(string identifier)
    {
        Identifier = identifier;
    }

    public void AddUsingNamespace(string ns) { }

    public void WriteOutput(IOutputContext outputContext)
    {
        for (var i = 0; i < AttributeLists.Count; i++)
        {
            AttributeLists[i].WriteOutput(outputContext);
        }
        for (var i = 0; i < Modifiers.Count; i++)
        {
            Modifiers[i].WriteOutput(outputContext);
        }
        Tk.W(outputContext, "enum");
        Tk.W(outputContext, Identifier);
        BaseList?.WriteOutput(outputContext);
        Tk.W(outputContext, "{");
        for (var i = 0; i < Members.Count; i++)
        {
            if (i > 0) outputContext.Write(", ");
            Members[i].WriteOutput(outputContext);
        }
        Tk.W(outputContext, "}");
        Tk.W(outputContext, ";");
    }
}

public sealed class Parameter : ISyntax
{
    public List<ISyntax> AttributeLists { get; } = new();
    public List<ISyntax> Modifiers { get; } = new();
    public ITypeDefinition? Type { get; set; }
    public string Identifier { get; set; }
    public ISyntax? Default { get; set; }

    public Parameter(string identifier)
    {
        Identifier = identifier;
    }

    public void AddUsingNamespace(string ns) { }

    public void WriteOutput(IOutputContext outputContext)
    {
        for (var i = 0; i < AttributeLists.Count; i++)
        {
            AttributeLists[i].WriteOutput(outputContext);
        }
        for (var i = 0; i < Modifiers.Count; i++)
        {
            Modifiers[i].WriteOutput(outputContext);
        }
        if (Type != null) outputContext.Write(Type);
        Tk.W(outputContext, Identifier);
        Default?.WriteOutput(outputContext);
    }
}
